# 2026-05-14 全局 Code Review 记录

## 项目功能概览

这是一个 PySide6 桌面宠物项目。核心功能包括透明置顶桌宠窗口、GIF 状态动画、鼠标拖拽和悬停交互、右键菜单、三属性衰减与 JSON 持久化、工作模式、系统托盘和 PyInstaller 打包发布。

## 已修复

1. **P1 - 打包版素材路径错误**
   - 位置：`config.py`, `.github/workflows/release.yml`
   - 问题：PyInstaller `--onefile --add-data "assets;assets"` 会把资源解到 `sys._MEIPASS`，原代码 frozen 模式下从 exe 目录找 `assets/gif`，发布版可能加载不到 GIF。
   - 修复：frozen 模式优先使用 `sys._MEIPASS` 作为资源根目录。

2. **P1 - `QMovie.loopCount` 用法无效**
   - 位置：`pet_window.py`
   - 问题：PySide6 的 `QMovie.loopCount` 是只读方法，`movie.loopCount = 1/-1` 不会控制播放次数；一次性动画实际依赖 5 秒超时结束。
   - 修复：一次性动画改为监听 `frameChanged`，到达最后一帧后停止并回到 idle；保留 5 秒超时作为兜底。

3. **P1 - 鼠标穿透开启后缺少恢复入口**
   - 位置：`pet_window.py`
   - 问题：右键菜单开启鼠标穿透后，宠物窗口不再接收鼠标事件；托盘菜单原本只有显示/隐藏和退出，用户可能无法关闭穿透。
   - 修复：托盘菜单在穿透开启时显示“关闭鼠标穿透”，在打工模式中显示“退出打工模式”；相关状态变化后刷新托盘菜单。

4. **P2 - 尺寸预设按钮、滑块和实际缩放状态不同步**
   - 位置：`pet_menu.py`, `pet_window.py`
   - 问题：点击“大/中/小”预设按钮只调用尺寸回调，不会同步下方滑块；窗口尺寸变化后，当前正在播放的 GIF 没有重新设置 `scaledSize`；靠近屏幕边缘放大时可能越界。
   - 修复：预设按钮统一驱动滑块，滑块再触发尺寸回调；尺寸标签实时显示当前 px；缩放时同步当前 movie 的 `scaledSize`，以中心为锚点调整，并 clamp 到屏幕可用区域内；GIF 缓存改为 `CacheNone`，避免变更尺寸后继续显示旧缓存帧。

5. **P2 - 一次性动画结束事件需要防止旧回调影响新交互**
   - 位置：`pet_window.py`
   - 问题：`finished` 回调没有绑定交互 ID，旧动画结束事件可能清掉当前交互状态。
   - 修复：`finished` 和最后一帧结束路径都携带 `interaction_id`，过期回调会被忽略。

6. **P2 - 打工模式会覆盖用户偏好和普通坐标**
   - 位置：`pet_window.py`
   - 问题：进入打工模式会强制 `topmost/click_through`，退出时没有恢复进入前偏好；如果在打工模式退出程序，普通 `x/y` 可能被工作模式坐标覆盖。
   - 修复：工作模式改为运行时有效置顶/穿透，不写入用户偏好；只有普通模式下才同步保存 `stats.x/stats.y`。

7. **P2 - 托盘图标复用 idle `QMovie` 可能影响当前动画**
   - 位置：`pet_window.py`
   - 问题：托盘图标从 `idle_gifs[0]` 取帧并调用 `stop()`，如果当前窗口也在播放同一个 QMovie，可能被停止。
   - 修复：托盘图标使用独立 `QMovie(idle_gif.fileName())` 实例，不再操作窗口动画实例。

8. **P2 - JSON 持久化加载缺少类型和值校验**
   - 位置：`pet_stats.py`
   - 问题：损坏 JSON 会被吞掉，但字段类型错误没有校验；例如字符串数值可能在 tick 减法时报错。
   - 修复：加载时对数值、布尔、尺寸、坐标和冷却时间做类型转换与范围 clamp；坏字段回退默认值。

9. **P3 - 单实例服务监听失败未处理**
   - 位置：`main.py`
   - 问题：`QLocalServer.listen("LineDogPet")` 未检查返回值，异常退出留下 stale server name 时可能导致单实例保护失效。
   - 修复：拆出 `start_local_server()`，监听失败时调用 `QLocalServer.removeServer()` 后重试，仍失败则抛出明确错误。

10. **P2 - 默认尺寸偏大**
    - 位置：`config.py`
    - 问题：首次启动默认 200px 过大。
    - 修复：默认尺寸调整为 180px，预设调整为小 120px、中 180px、大 270px。

11. **P2 - 首次使用好感度未满**
    - 位置：`pet_stats.py`
    - 问题：首次使用时好感度默认 50，不符合三项能力值满值的预期。
    - 修复：首次使用和坏数据回退时好感度默认 100。

12. **P1 - 工作模式下无法右键退出**
    - 位置：`pet_window.py`
    - 问题：工作模式强制鼠标穿透，导致小狗本体无法右键呼出菜单；托盘退出路径也容易受状态刷新影响。
    - 修复：工作模式不再强制鼠标穿透，仅保持小尺寸和置顶；右键菜单和托盘菜单都可退出打工模式。

13. **P2 - 互动动画太短且容易被打断**
    - 位置：`config.py`, `pet_window.py`
    - 问题：互动兜底时长只有 5 秒，且 idle/walk 等状态切换仍可能覆盖当前互动。
    - 修复：互动动画至少播放满 5 秒；GIF 提前到最后一帧也不会立刻切回 idle；互动期间 idle、walk、hover greet 和其他互动入口都不会打断当前互动。

14. **Feature - 启动问候和高好感 happy 定时动画**
    - 位置：`config.py`, `pet_window.py`
    - 需求：每次登录随机调用 `greet` 动画；好感度按区间决定 `happy`/`idle`/`angry` 的心情播放节奏。
    - 实现：启动后直接播放一次 `greet`；好感度大于 90 时每 1 分钟从 `idle`/`happy` 随机选择，`happy` 播放 1 分钟；80-90 时每 20 秒 50% 概率播放 `happy` 5 秒；50-80 只 idle；低于 50 时每 20 秒 50% 概率播放 `angry` 5 秒。

15. **P2 - 首次默认位置不应依赖持久化**
    - 位置：`pet_stats.py`
    - 问题：无存档首次启动时使用固定坐标，不能稳定出现在桌面右下角。
    - 修复：仅无存档时使用默认位置：桌面右下角，右侧 margin 为屏幕宽度 10%；有存档时继续使用上次保存的位置和尺寸。

16. **P2 - 工作模式下仍可能触发随机移动**
    - 位置：`pet_window.py`
    - 问题：随机移动逻辑没有排除工作模式，可能把右下角打工小狗移走。
    - 修复：工作模式下 `_tick` 保留属性衰减，但跳过情绪/空闲/随机移动动画。

## 其余发现

暂无。

## 回归测试

新增 `test_regressions.py` 覆盖：

- frozen 模式下资源路径使用 `sys._MEIPASS`
- 过期交互结束事件不会清掉当前交互
- 托盘菜单可恢复鼠标穿透和打工模式
- 运行时开启鼠标穿透会刷新托盘恢复入口
- 尺寸预设按钮会同步滑块和尺寸回调
- 缩放会同步当前 GIF 渲染尺寸并保持窗口在屏幕内
- 打工模式不覆盖用户置顶/穿透偏好，也不覆盖普通模式坐标
- 托盘图标使用独立 `QMovie`，不会停止当前窗口动画
- JSON 加载会清洗坏类型和越界值
- 单实例服务监听失败会清理 stale server name 后重试
- 默认尺寸 180px，首次默认位置为右下角 10% 右边距
- 首次使用三项能力值均为 100
- 工作模式下小狗本体仍可右键，托盘退出打工模式可用
- 互动动画至少播放 5 秒，互动中不会被 idle/walk 打断
- 启动后直接播放 `greet`，好感度按 90/80/50 三档调度 happy/idle/angry
- 随机移动触发条件有测试覆盖，且工作模式不会随机移动
